@main
def collections(): Unit = {
    // List is a oredred collectiong of elements
    // can be accessed by the position (index)
    val a = List(1, 2, 3, 632, 9, 52, 6)
    val b = List(1, "hello", true)
    val c = List()

    // Mutable vs Immutable 
}